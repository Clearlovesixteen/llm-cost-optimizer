---
name: llm-cost-optimizer
description: 审计并优化 LLM API、Agent、MCP、RAG 与 AI 编程工作流的 Token 用量和端到端成本。当用户要求降本、缩短上下文、优化缓存或工具载荷、调整模型路由、控制重试或多 Agent 开销，或评估成本优化方案时使用。默认只分析并给出方案，未获明确授权时不修改系统。
---

# LLM Cost Optimizer

把优化目标定义为：在质量、安全与时延要求达标的前提下，降低每个验收通过任务的总成本。

## 工作边界

- 区分三类目标：减少 Token、降低费用、降低时延。缓存折扣或批处理可能只降费用；流式与并发可能只改善等待体验。
- 核算完整链路：成功与失败尝试、重试、子 Agent、模型复核、工具和相关基础设施。不要只看最后一次成功调用。
- 以证据为依据。缺少用量、价格、成功率或链路信息时标记未知并给出采集办法，不编造基线或节省比例。
- 供应商价格、缓存计费、上下文续接和服务能力会变化。作定量结论前，查询当前官方文档并注明价格日期、币种和适用条件。
- 默认交付审计与优先级方案。只有用户明确要求实施时，才修改提示词、代码、配置或基础设施。

## 审计流程

1. **确定单位与约束**：明确任务类型、验收标准、质量与安全门槛、时延目标、流量、模型/供应商和可用数据。
2. **建立基线**：优先计算单位验收通过任务成本、成功率、P50/P95 时延、调用与重试分布，以及输入、缓存输入、输出和其他计费项。
3. **定位浪费**：先查重复工作、无关上下文、过大工具返回、失败空转和无效多 Agent 扇出，再查缓存结构、模型选择、推理力度与输出长度。
4. **提出改造**：说明每项建议影响 Token、费用还是时延；列出证据、预期方向、风险、依赖、置信度和验证方法，避免重复计算同一份收益。
5. **排序与停止**：按预期价值、改造成本、质量风险和证据置信度排序。证据足够且交付物完整后停止，不重复给出同义建议。

对实际系统做全面审计、设计埋点或比较多个方案时，读取 [references/audit-playbook.md](references/audit-playbook.md)。概念性短问答无需加载整份手册。

## 交付格式

根据任务规模裁剪，但通常包含：

1. 结论：最大浪费来源与最值得先做的动作。
2. 基线与缺口：已知指标、数据口径、未知项。
3. 发现：问题、证据、成本机制、建议、影响对象、风险与置信度。
4. 优先级计划：低风险快赢、中期改造、条件满足后再做的项目。
5. 验证方案：对照组、质量/安全守门指标、成本和时延指标、停止或回滚条件。

若验收通过数为零，单位成功成本标记为不可计算，不写成零。若只能判断方向而不能量化，明确写“方向性判断”。

## 实施模式

用户明确要求落地时：

- 先保留可比较的基线和验收门槛，再做最小范围改动。
- 把数据过滤、排序、聚合和格式转换放在程序侧；模型负责理解、例外处理与解释。
- 保留必要权限检查、原始证据和测试门禁，不用降质或跳过安全检查换取表面节省。
- 通过相同任务集比较改造前后；同时报告成本、成功率、质量/安全结果和 P95 时延。
